#!/bin/bash
# this line is used to describe the script

clear # clears the screen
username="arun"  	# the username for this application
echo "we are using the default user called :$username" #displays the console
datestamp=`date` 
echo " this is the time when the script run :$datestamp"
